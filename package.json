{
  "name": "preview-buddy",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=@preview-buddy/server",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo build",
    "test:e2e": "bun run e2e/run.ts",
    "test:e2e:fixtures": "bun test e2e/fixtures.test.ts",
    "db:generate": "bun run --cwd apps/server db:generate",
    "db:migrate": "bun run --cwd apps/server db:migrate",
    "db:studio": "bun run --cwd apps/server db:studio"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.4.0"
}
